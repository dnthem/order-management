const API_URL = import.meta.env.VITE_API_URL;
const BASE_URL = import.meta.env.VITE_BASE_URL;
const ORIGIN = import.meta.env.VITE_ORIGIN;

const OrderCardStyle = {
  width: "auto",
  maxWidth: "18rem",
  margin: ".5rem",
};

export { API_URL, BASE_URL, ORIGIN, OrderCardStyle };
